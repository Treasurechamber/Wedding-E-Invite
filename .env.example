# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
# Required for Master save & setup-master (Supabase → Settings → API → service_role)
# Must be set in Vercel env for save to work on production
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
